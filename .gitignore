# Composer dependencies (not distributed).
vendor

# Frontend build dependencies (not distributed).
app/node_modules
app/dist

# Local dev data (not distributed).
cohort_plugins.csv

# Local packaging helper.
package.sh

# Generated packaging artifacts.
*.zip
